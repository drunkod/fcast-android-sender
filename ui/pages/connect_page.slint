// connect_page.slint — Transparent placeholder shown when AppState == Disconnected.
// Connection UI lives in FullSettingsPage (settings_page.slint) → RECEIVER section.

export component ConnectView inherits Rectangle {
    background: transparent;
}
